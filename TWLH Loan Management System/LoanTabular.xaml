<Page x:Class="TWLH_Loan_Management_System.LoanTabular"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TWLH_Loan_Management_System"
      xmlns:fa="http://schemas.fontawesome.io/icons/"
      mc:Ignorable="d" 
      d:DesignHeight="650" d:DesignWidth="1100"
      Title="LoanTabular">

    <Grid Background="Transparent">
        <Border Background="White" CornerRadius="15" BorderBrush="#E2E8F0" BorderThickness="1" Padding="10">
            <DataGrid x:Name="dgLoans" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True" 
                      SelectionMode="Single"
                      RowHeight="55"
                      Background="Transparent"
                      BorderThickness="0"
                      GridLinesVisibility="None"
                      HeadersVisibility="Column"
                      FontSize="13"
                      CanUserAddRows="False"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto"
                      SelectionChanged="DataGrid_SelectionChanged">
                <DataGrid.Resources>
                    <Style x:Key="ActionButtonStyle" TargetType="Button">
                        <Setter Property="Height" Value="32"/>
                        <Setter Property="Padding" Value="18,0"/>
                        <Setter Property="MinWidth" Value="85"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                    
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F8FAFC"/>
                        <Setter Property="Foreground" Value="#64748B"/>
                        <Setter Property="Padding" Value="15,12"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#E2E8F0"/>
                    </Style>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#F1F5F9"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F8FAFC"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#E0E7FF"/>
                                <Setter Property="Foreground" Value="#1E293B"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="15,0"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </DataGrid.Resources>
                
                <DataGrid.Columns>
                    <DataGridTextColumn Header="LOAN ID" Binding="{Binding loan_id, Mode=OneWay}" Width="80"/>
                    <DataGridTemplateColumn Header="CLIENT" Width="2*" MinWidth="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{Binding FullName, Mode=OneWay}" FontWeight="SemiBold" Foreground="#1E293B"/>
                                    <TextBlock Text="{Binding client_id, Mode=OneWay, StringFormat='ID: \{0\}'}" FontSize="11" Foreground="#64748B"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="CREATED" Binding="{Binding created_at, Mode=OneWay, StringFormat='\{0:MMM dd, yyyy\}'}" Width="*" MinWidth="110"/>
                    <DataGridTextColumn Header="AMOUNT" Binding="{Binding loan_amount, Mode=OneWay, StringFormat='₱\{0:N2\}'}" Width="*" MinWidth="110"/>
                    <DataGridTextColumn Header="INTEREST" Binding="{Binding interest_rate, Mode=OneWay, StringFormat='\{0\}%'}" Width="80"/>
                    <DataGridTextColumn Header="DUE DATE" Binding="{Binding due_date, Mode=OneWay, StringFormat='\{0:MMM dd, yyyy\}'}" Width="*" MinWidth="110"/>
                    <DataGridTextColumn Header="PLAN" Binding="{Binding installment_plan, Mode=OneWay}" Width="*" MinWidth="100"/>
                    <DataGridTemplateColumn Header="STATUS" Width="120" MinWidth="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Padding="8,4" CornerRadius="6" HorizontalAlignment="Left">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding loan_status, Mode=OneWay}" Value="Active">
                                                    <Setter Property="Background" Value="#E0E7FF"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding loan_status, Mode=OneWay}" Value="Paid">
                                                    <Setter Property="Background" Value="#D1FAE5"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding loan_status, Mode=OneWay}" Value="Past Due">
                                                    <Setter Property="Background" Value="#FEE2E2"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding loan_status, Mode=OneWay}" FontSize="11" FontWeight="Bold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding loan_status, Mode=OneWay}" Value="Active">
                                                        <Setter Property="Foreground" Value="#3044FF"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding loan_status, Mode=OneWay}" Value="Paid">
                                                        <Setter Property="Foreground" Value="#10B981"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding loan_status, Mode=OneWay}" Value="Past Due">
                                                        <Setter Property="Foreground" Value="#EF4444"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="PROGRESS" Width="1.5*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{Binding ProgressText, Mode=OneWay}" FontSize="11" HorizontalAlignment="Right" Margin="0,0,0,2"/>
                                    <ProgressBar Value="{Binding ProgressValue, Mode=OneWay}" Height="6" Maximum="100" BorderThickness="0" Background="#E2E8F0" Foreground="#3044FF"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <DataGridTemplateColumn Header="ACTIONS" Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Button Style="{StaticResource ActionButtonStyle}" 
                                            Background="#F1F5F9" Foreground="#1E293B" 
                                            Margin="0,0,8,0" Click="btnView_Click">
                                        <StackPanel Orientation="Horizontal">
                                            <fa:ImageAwesome Icon="Eye" Width="12" Height="12" Margin="0,0,5,0" Foreground="#1E293B"/>
                                            <TextBlock Text="View" VerticalAlignment="Center" FontSize="11"/>
                                        </StackPanel>
                                    </Button>
                                    <Button Style="{StaticResource ActionButtonStyle}" 
                                            Background="#3044FF" Foreground="White" Click="btnUpdate_Click">
                                        <StackPanel Orientation="Horizontal">
                                            <fa:ImageAwesome Icon="Edit" Width="12" Height="12" Margin="0,0,5,0" Foreground="White"/>
                                            <TextBlock Text="Edit" VerticalAlignment="Center" FontSize="11"/>
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
