<Window x:Class="TWLH_Loan_Management_System.PenaltyInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TWLH_Loan_Management_System"
        mc:Ignorable="d"
        Title="Enter Penalty" Height="220" Width="350"
        WindowStartupLocation="CenterScreen" WindowStyle="None" 
        AllowsTransparency="True" Background="Transparent">
    
    <Border Background="White" CornerRadius="15" BorderThickness="1" BorderBrush="#E2E8F0">
        <StackPanel Margin="25">
            <TextBlock Text="Add Penalty Amount" FontSize="18" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,10"/>
            <TextBlock Text="Enter the penalty amount for this past due installment." FontSize="12" Foreground="#64748B" TextWrapping="Wrap" Margin="0,0,0,20"/>
            
            <TextBox x:Name="txtPenalty" Height="40" Padding="10,5" VerticalContentAlignment="Center" FontSize="16" FontWeight="SemiBold">
                <TextBox.Resources>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </TextBox.Resources>
            </TextBox>

            <Grid Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Button Content="Cancel" Grid.Column="0" Margin="0,0,5,0" Height="35" Click="btnCancel_Click" Background="#F1F5F9" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
                
                <Button Content="Confirm" Grid.Column="1" Margin="5,0,0,0" Height="35" Click="btnConfirm_Click" Background="#EF4444" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </StackPanel>
    </Border>
</Window>
