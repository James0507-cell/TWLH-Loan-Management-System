<Window x:Class="TWLH_Loan_Management_System.ClientDetail"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TWLH_Loan_Management_System"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        mc:Ignorable="d"
        Title="Client Profile Details" Height="800" Width="1000"
        WindowStartupLocation="CenterScreen" WindowStyle="None" 
        AllowsTransparency="True" Background="Transparent">

    <Border Background="#F8FAFC" CornerRadius="20" BorderThickness="1" BorderBrush="#E2E8F0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header Section -->
            <Border Grid.Row="0" Background="#6366F1" CornerRadius="20,20,0,0" Padding="30,25">
                <Grid>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock x:Name="txtFullName" Text="Juan Dela Cruz" FontSize="28" FontWeight="Bold" Foreground="White"/>
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <Border Background="#4F46E5" CornerRadius="4" Padding="8,3" Margin="0,0,10,0">
                                <TextBlock x:Name="txtClientID" Text="ID: #0001" FontSize="12" FontWeight="SemiBold" Foreground="White"/>
                            </Border>
                            <TextBlock x:Name="txtGender" Text="Male" FontSize="14" Foreground="#E0E7FF" VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>

                    <Button HorizontalAlignment="Right" VerticalAlignment="Top" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="btnClose_Click">
                        <fa:ImageAwesome Icon="Close" Width="16" Height="16" Foreground="White"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Content Area -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="30">
                <StackPanel>
                    <!-- Personal & Contact Info Grid -->
                    <Grid Margin="0,0,0,30">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Basic Details Card -->
                        <Border Grid.Column="0" Background="White" CornerRadius="15" Padding="25" Margin="0,0,20,0">
                            <StackPanel>
                                <TextBlock Text="Contact Information" FontSize="16" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,20"/>
                                
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <fa:ImageAwesome Icon="Phone" Grid.Row="0" Grid.Column="0" Width="14" Foreground="#64748B" Margin="0,0,15,15"/>
                                    <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,15">
                                        <TextBlock Text="Phone Number" FontSize="11" Foreground="#94A3B8"/>
                                        <TextBlock x:Name="txtPhone" Text="0917 123 4567" FontSize="14" Foreground="#334155"/>
                                    </StackPanel>

                                    <fa:ImageAwesome Icon="Facebook" Grid.Row="1" Grid.Column="0" Width="14" Foreground="#64748B" Margin="0,0,15,15"/>
                                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,0,15">
                                        <TextBlock Text="Messenger" FontSize="11" Foreground="#94A3B8"/>
                                        <TextBlock x:Name="txtMessenger" Text="juan.dc.99" FontSize="14" Foreground="#334155"/>
                                    </StackPanel>

                                    <fa:ImageAwesome Icon="MapMarker" Grid.Row="2" Grid.Column="0" Width="14" Foreground="#64748B" Margin="0,0,15,0"/>
                                    <StackPanel Grid.Row="2" Grid.Column="1">
                                        <TextBlock Text="Residence" FontSize="11" Foreground="#94A3B8"/>
                                        <TextBlock x:Name="txtResidence" Text="Davao City, Philippines" FontSize="14" Foreground="#334155" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- Date Details Card -->
                        <Border Grid.Column="1" Background="White" CornerRadius="15" Padding="25">
                            <StackPanel>
                                <TextBlock Text="Key Dates" FontSize="16" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,20"/>

                                <StackPanel Margin="0,0,0,15">
                                    <TextBlock Text="Date of Birth" FontSize="11" Foreground="#94A3B8"/>
                                    <TextBlock x:Name="txtBirthDate" Text="May 12, 1998" FontSize="14" Foreground="#334155"/>
                                </StackPanel>

                                <StackPanel>
                                    <TextBlock Text="Member Since" FontSize="11" Foreground="#94A3B8"/>
                                    <TextBlock x:Name="txtJoinedDate" Text="Feb 11, 2026" FontSize="14" Foreground="#334155"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <!-- Businesses Section -->
                    <StackPanel Margin="0,0,0,30">
                        <Grid Margin="0,0,0,15">
                            <TextBlock Text="Registered Businesses" FontSize="18" FontWeight="Bold" Foreground="#1E293B" VerticalAlignment="Center"/>
                            <Button x:Name="btnAddBusiness" Content="+ Add Business" HorizontalAlignment="Right" Background="#6366F1" Foreground="White" Padding="12,6" BorderThickness="0" Cursor="Hand" Click="btnAddBusiness_Click">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="6"/>
                                    </Style>
                                </Button.Resources>
                            </Button>
                        </Grid>
                        <WrapPanel x:Name="businessContainer" Margin="-10,0">
                            <!-- Business Cards loaded here -->
                        </WrapPanel>
                    </StackPanel>

                    <!-- Loans Section -->
                    <StackPanel>
                        <TextBlock Text="Loan History" FontSize="18" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,15"/>
                        <WrapPanel x:Name="loanContainer" Margin="-10,0">
                            <!-- Loan Cards loaded here -->
                        </WrapPanel>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
