<Window x:Class="TWLH_Loan_Management_System.EmployeeForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        Title="Employee Form" Height="750" Width="520" 
        WindowStartupLocation="CenterOwner" WindowStyle="None" 
        AllowsTransparency="True" Background="Transparent" ResizeMode="NoResize">

    <Window.Resources>
        <!-- Modern Input Style -->
        <Style x:Key="ModernInput" TargetType="Control">
            <Setter Property="Height" Value="42"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Resources>
                <Style TargetType="{x:Type Border}">
                    <Setter Property="CornerRadius" Value="8"/>
                </Style>
            </Style.Resources>
        </Style>

        <!-- Elegant ComboBox Style -->
        <Style x:Key="ElegantComboBox" TargetType="ComboBox">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10"/>
                            <ToggleButton x:Name="toggleButton" BorderThickness="0" Background="Transparent" ClickMode="Press" Focusable="false" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent">
                                            <Path x:Name="Arrow" Data="M0,0 L4,4 L8,0 Z" Fill="#64748B" HorizontalAlignment="Right" Margin="0,0,15,0" VerticalAlignment="Center" Stretch="Uniform" Width="10"/>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="contentPresenter" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="{TemplateBinding Padding}" VerticalAlignment="Center" IsHitTestVisible="false"/>
                            <Popup x:Name="PART_Popup" AllowsTransparency="true" Grid.ColumnSpan="2" IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}" Placement="Bottom" PopupAnimation="Slide">
                                <Border x:Name="dropDownBorder" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" MaxHeight="{TemplateBinding MaxDropDownHeight}" MinWidth="{Binding ActualWidth, ElementName=border}" Margin="0,2,0,0">
                                    <ScrollViewer x:Name="DropDownScrollViewer">
                                        <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#CBD5E0"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#6366F1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="20" BorderThickness="1" BorderBrush="#E2E8F0" ClipToBounds="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#F8FAFC" Padding="30,25" BorderThickness="0,0,0,1" CornerRadius="20,20,0,0" BorderBrush="#F1F5F9">
                <StackPanel>
                    <Grid>
                        <TextBlock x:Name="TitleText" Text="Add New Employee" FontSize="22" FontWeight="Bold" Foreground="#1E293B"/>
                        <Button HorizontalAlignment="Right" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="btnCancel_Click">
                            <fa:ImageAwesome Icon="Close" Width="14" Height="14" Foreground="#64748B"/>
                        </Button>
                    </Grid>
                    <TextBlock Text="Manage team members and their system access." FontSize="13" Foreground="#64748B" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Form Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="30,25">
                <StackPanel>
                    <!-- Personal Info Section -->
                    <TextBlock Text="PERSONAL INFORMATION" FontSize="11" FontWeight="Bold" Foreground="#6366F1" Margin="0,0,0,15"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="First Name" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,0,0,8"/>
                            <TextBox x:Name="txtFirstName" Style="{StaticResource ModernInput}" Margin="0,0,0,15"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="Last Name" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,0,0,8"/>
                            <TextBox x:Name="txtLastName" Style="{StaticResource ModernInput}" Margin="0,0,0,15"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Middle Name" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,0,0,8"/>
                    <TextBox x:Name="txtMiddleName" Style="{StaticResource ModernInput}" Margin="0,0,0,15"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="Gender" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,0,0,8"/>
                            <ComboBox x:Name="cmbGender" Style="{StaticResource ElegantComboBox}" Margin="0,0,0,15">
                                <ComboBoxItem Content="Male"/>
                                <ComboBoxItem Content="Female"/>
                                <ComboBoxItem Content="Others"/>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="Date of Birth" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,0,0,8"/>
                            <DatePicker x:Name="dpBirthDate" Height="42" Padding="5,0" VerticalContentAlignment="Center" Margin="0,0,0,15"/>
                        </StackPanel>
                    </Grid>

                    <Separator Margin="0,15,0,20" Background="#F1F5F9"/>

                    <!-- Role & Contact Section -->
                    <TextBlock Text="ROLE &amp; CONTACT" FontSize="11" FontWeight="Bold" Foreground="#6366F1" Margin="0,0,0,15"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="Role" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,0,0,8"/>
                            <ComboBox x:Name="cmbRole" Style="{StaticResource ElegantComboBox}" Margin="0,0,0,15">
                                <ComboBoxItem Content="Admin"/>
                                <ComboBoxItem Content="Staff"/>
                                <ComboBoxItem Content="Loan Collector"/>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="Contact Number" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,0,0,8"/>
                            <TextBox x:Name="txtContact" Style="{StaticResource ModernInput}" Margin="0,0,0,15"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Email Address" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,0,0,8"/>
                    <TextBox x:Name="txtEmail" Style="{StaticResource ModernInput}" Margin="0,0,0,15"/>

                    <TextBlock Text="Employee Status" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,0,0,8"/>
                    <ComboBox x:Name="cmbEmployeeStatus" Style="{StaticResource ElegantComboBox}" Margin="0,0,0,15">
                        <ComboBoxItem Content="Active" IsSelected="True"/>
                        <ComboBoxItem Content="Inactive"/>
                    </ComboBox>

                    <Separator Margin="0,15,0,20" Background="#F1F5F9"/>

                    <!-- Credentials Section -->
                    <TextBlock Text="ACCOUNT CREDENTIALS" FontSize="11" FontWeight="Bold" Foreground="#6366F1" Margin="0,0,0,15"/>

                    <TextBlock Text="Credential Status" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,0,0,8"/>
                    <ComboBox x:Name="cmbCredentialStatus" Style="{StaticResource ElegantComboBox}" Margin="0,0,0,15">
                        <ComboBoxItem Content="Active" IsSelected="True"/>
                        <ComboBoxItem Content="Inactive"/>
                    </ComboBox>

                    <TextBlock Text="Username" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,0,0,8"/>
                    <TextBox x:Name="txtUsername" Style="{StaticResource ModernInput}" Margin="0,0,0,15"/>

                    <StackPanel x:Name="pnlPassword">
                        <TextBlock x:Name="lblPassword" Text="Password" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,0,0,8"/>
                        <PasswordBox x:Name="txtPassword" Height="42" Padding="10,0" VerticalContentAlignment="Center" Margin="0,0,0,15">
                            <PasswordBox.Resources>
                                <Style TargetType="{x:Type Border}">
                                    <Setter Property="CornerRadius" Value="10"/>
                                </Style>
                            </PasswordBox.Resources>
                        </PasswordBox>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>

            <!-- Footer -->
            <Border Grid.Row="2" Background="#F8FAFC" Padding="30,20" BorderThickness="0,1,0,0" CornerRadius="0,0,20,20" BorderBrush="#F1F5F9">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Content="Cancel" Grid.Column="0" Height="45" Margin="0,0,10,0" Background="White" Foreground="#64748B" BorderBrush="#E2E8F0" BorderThickness="1" Cursor="Hand" Click="btnCancel_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button x:Name="btnSave" Content="Save Employee" Grid.Column="1" Height="45" Background="#4F46E5" Foreground="White" BorderThickness="0" FontWeight="Bold" Cursor="Hand" Click="btnSave_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="10">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
