<Window x:Class="TWLH_Loan_Management_System.LoanDetails"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TWLH_Loan_Management_System"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        mc:Ignorable="d"
        Title="Loan Details" Height="650" Width="725" WindowStyle="None" 
        AllowsTransparency="True" Background="Transparent" WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <!-- Custom Card Button Style -->
        <Style x:Key="CardButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="38"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="20" BorderThickness="0" ClipToBounds="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border BorderThickness="0,0,0,1" BorderBrush="#E2E8F0" Background="#FFF2F6FF" CornerRadius="20,20,0,0" Padding="30,25">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel>
                        <TextBlock x:Name="txtLoanID" Text="Loan #000" FontSize="24" FontWeight="Bold" Foreground="#1E293B"/>
                        <TextBlock x:Name="txtClientID" Text="Client ID: 000" FontSize="14" Foreground="#64748B" Margin="0,5,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,40,0">
                        <StackPanel Margin="0,0,25,0">
                            <TextBlock Text="PLAN" FontSize="10" FontWeight="SemiBold" Foreground="#64748B" HorizontalAlignment="Right"/>
                            <TextBlock x:Name="txtPlan" Text="Monthly" FontSize="15" FontWeight="Bold" Foreground="#1E293B" HorizontalAlignment="Right"/>
                        </StackPanel>
                        <StackPanel>
                            <TextBlock Text="INTEREST" FontSize="10" FontWeight="SemiBold" Foreground="#64748B" HorizontalAlignment="Right"/>
                            <TextBlock x:Name="txtInterest" Text="5.00%" FontSize="15" FontWeight="Bold" Foreground="#1E293B" HorizontalAlignment="Right"/>
                        </StackPanel>
                    </StackPanel>

                    <Button HorizontalAlignment="Right" VerticalAlignment="Top" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="btnClose_Click" Grid.Column="1" Margin="0,-5,0,0">
                        <fa:ImageAwesome Icon="Close" Width="14" Height="14" Foreground="#64748B"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Main Content -->
            <ScrollViewer x:Name="scrLoanInstallment" Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="installmentContainer" Margin="30,20"/>
            </ScrollViewer>

            <!-- Footer -->
            <Border Grid.Row="2" BorderThickness="0,1,0,0" BorderBrush="#E2E8F0" Background="#FFF2F6FF" CornerRadius="0,0,20,20" Padding="30,20">
                <Grid>
                    <TextBlock Text="GRAND TOTAL REMAINING" VerticalAlignment="Center" FontSize="14" FontWeight="Bold" Foreground="#64748B"/>
                    <TextBlock x:Name="txtGrandTotal" Text="₱0.00" HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="26" FontWeight="Bold" Foreground="#3044FF"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
