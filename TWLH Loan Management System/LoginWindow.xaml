<Window x:Class="TWLH_Loan_Management_System.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TWLH_Loan_Management_System"
        mc:Ignorable="d"
        Title="Login - TWLH Loan Management System" Height="600" Width="1000" 
        WindowStartupLocation="CenterScreen" Background="#F0F2F5">

    <Window.Resources>
        <!-- Modern Color Palette -->
        <SolidColorBrush x:Key="AccentBlue" Color="#3044FF"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#1E293B"/>
        <SolidColorBrush x:Key="TextMuted" Color="#64748B"/>
        <SolidColorBrush x:Key="SidebarBack" Color="#FFFFFF"/>

        <Geometry x:Key="IconCompany">M12,7V9H2V22H22V7H12M10,20H4V18H10V20M10,16H4V14H10V16M10,12H4V10H10V12M20,20H14V18H20V20M20,16H14V14H20V16M20,12H14V10H20V12M20,8H14V5H20V8Z</Geometry>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="0,5,0,15"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Name="Border" CornerRadius="8" BorderThickness="1" BorderBrush="#E2E8F0" Background="White">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
                                <Setter TargetName="Border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ModerPasswordBox" TargetType="TextBox">
    <Setter Property="Padding" Value="10,8"/>
    <Setter Property="Margin" Value="0,5,0,15"/>
    <Setter Property="FontSize" Value="14"/>
    <Setter Property="Template">
        <Setter.Value>
            <ControlTemplate TargetType="TextBox">
                <Border Name="Border" CornerRadius="8" BorderThickness="1" BorderBrush="#E2E8F0" Background="White">
                    <ScrollViewer x:Name="PART_ContentHost"/>
                </Border>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsFocused" Value="True">
                        <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
                        <Setter TargetName="Border" Property="BorderThickness" Value="2"/>
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>
        </Setter.Value>
    </Setter>
</Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBlue}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Column: Branding -->
        <Border Grid.Column="0" Background="{StaticResource AccentBlue}">
            <StackPanel VerticalAlignment="Center" Margin="60">
                <Path Data="{StaticResource IconCompany}" Fill="White" Width="64" Height="64" Stretch="Uniform" HorizontalAlignment="Left" Margin="0,0,0,20"/>
                <TextBlock Text="TWLH" Foreground="White" FontSize="48" FontWeight="ExtraBold"/>
                <TextBlock Text="LOAN MANAGEMENT SYSTEM" Foreground="White" FontSize="18" FontWeight="SemiBold" Opacity="0.8" Margin="0,0,0,30"/>
                <TextBlock Text="Empowering your financial future with secure, efficient, and transparent loan management solutions." 
                           Foreground="White" FontSize="16" TextWrapping="Wrap" Opacity="0.9" LineHeight="24"/>
            </StackPanel>
        </Border>

        <!-- Right Column: Login Form -->
        <Border Grid.Column="1" Background="White">
            <StackPanel VerticalAlignment="Center" Margin="60">
                <TextBlock Text="Welcome Back" Foreground="{StaticResource TextPrimary}" FontSize="32" FontWeight="Bold" Margin="0,0,0,10"/>
                <TextBlock Text="Please sign in to your account" Foreground="{StaticResource TextMuted}" FontSize="16" Margin="0,0,0,40"/>

                <TextBlock Text="Username" Foreground="{StaticResource TextPrimary}" FontWeight="SemiBold" FontSize="14"/>
                <TextBox x:Name="txtUsername" Style="{StaticResource ModernTextBox}"/>

                <TextBlock Text="Password" Foreground="{StaticResource TextPrimary}" FontWeight="SemiBold" FontSize="14"/>
                <PasswordBox x:Name="txtPassword"  Padding="10,8" Margin="0,5,0,30" FontSize="14" Height="40">

                </PasswordBox>

                <Button x:Name="btnLogin" Content="Sign In" Style="{StaticResource PrimaryButtonStyle}" Click="Button_Click"/>
                
                <TextBlock Text="Forgot Password?" Foreground="{StaticResource AccentBlue}" HorizontalAlignment="Center" Margin="0,20,0,0" Cursor="Hand" FontWeight="Medium"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
