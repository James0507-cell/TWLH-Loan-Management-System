<Page x:Class="TWLH_Loan_Management_System.followUps"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:fa="http://schemas.fontawesome.io/icons/"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1000"
      Title="Follow-up Log"
      Background="#F8F9FA">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,25">
            <StackPanel>
                <TextBlock Text="Follow-up Log" FontSize="28" FontWeight="Bold" Foreground="#111827"/>
                <TextBlock Text="Track all collection activities and communications" Foreground="#6B7280" FontSize="14"/>
            </StackPanel>
        </DockPanel>

        <!-- Search & Add Bar -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="12" Padding="15,10" Margin="0,0,15,0" BorderBrush="#E5E7EB" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <fa:ImageAwesome Icon="Search" Width="16" Height="16" Foreground="#9CA3AF" Margin="5,0,15,0"/>
                    <TextBox x:Name="txtSearch" Grid.Column="1" Background="Transparent" BorderThickness="0" 
                             VerticalContentAlignment="Center" FontSize="14" Tag="Search follow-ups by client name or notes..." 
                             TextChanged="txtSearch_TextChanged"/>
                </Grid>
            </Border>

            <Button x:Name="btnAddFollowUp" Grid.Column="1" Content="+ Log Follow-up" Padding="20,0" 
                    Background="#4F46E5" Foreground="White" BorderThickness="0" Height="45" FontWeight="SemiBold" Click="btnAddFollowUp_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="10"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>

        <!-- Cards Container -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <WrapPanel x:Name="FollowUpCardsContainer" HorizontalAlignment="Center"/>
        </ScrollViewer>
    </Grid>
</Page>