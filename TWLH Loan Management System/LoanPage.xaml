<Page x:Class="TWLH_Loan_Management_System.LoanPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TWLH_Loan_Management_System"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        mc:Ignorable="d"
        Title="loanPage" d:DesignHeight="650" d:DesignWidth="840" Background="#FFF2F4F7" Loaded="Page_Loaded">
    <Page.Resources>
        <!-- Modern Search Box Style -->
        <Style x:Key="ModernSearchBox" TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <fa:ImageAwesome Icon="Search" Width="14" Height="14" Foreground="#A0AEC0" Margin="15,0,0,0" VerticalAlignment="Center"/>
                                <ScrollViewer x:Name="PART_ContentHost" Grid.Column="1" VerticalAlignment="Center" Margin="10,0,15,0"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Elegant ComboBox Style -->
        <Style x:Key="ElegantComboBox" TargetType="ComboBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10"/>
                            <ToggleButton x:Name="toggleButton" BorderThickness="0" Background="Transparent" ClickMode="Press" Focusable="false" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent">
                                            <Path x:Name="Arrow" Data="M0,0 L4,4 L8,0 Z" Fill="#64748B" HorizontalAlignment="Right" Margin="0,0,12,0" VerticalAlignment="Center" Stretch="Uniform" Width="8"/>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="contentPresenter" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="{TemplateBinding Padding}" VerticalAlignment="Center" IsHitTestVisible="false"/>
                            <Popup x:Name="PART_Popup" AllowsTransparency="true" Grid.ColumnSpan="2" IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}" Placement="Bottom" PopupAnimation="Slide">
                                <Border x:Name="dropDownBorder" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" MaxHeight="{TemplateBinding MaxDropDownHeight}" MinWidth="{Binding ActualWidth, ElementName=border}">
                                    <ScrollViewer x:Name="DropDownScrollViewer">
                                        <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#CBD5E0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Elegant Primary Button Style -->
        <Style x:Key="ElegantPrimaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Background" Value="#3044FF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="18,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2537E6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Custom Card Button Style -->
        <Style x:Key="CardButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="38"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" MinHeight="140"/>
            <RowDefinition Height="Auto" MinHeight="55"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="30,30,30,20">
            <TextBlock Text="Loans &amp; Schedules" FontSize="26" FontWeight="Bold" Foreground="#1E293B"/>
            <TextBlock Text="Manage client accounts, monitor repayments, and schedule new loans." FontSize="14" Foreground="#64748B" Margin="0,4,0,0"/>
            <Separator Background="#E2E8F0" Height="1" Margin="0,15,0,0"/>
        </StackPanel>

        <!-- Unified Actions Bar -->
        <Grid Margin="30,0,30,0" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto" MinWidth="164"/>
            </Grid.ColumnDefinitions>

            <!-- Filters & Search -->
            <StackPanel Orientation="Horizontal">
                <!-- Search -->
                <Grid Width="250">
                    <TextBox Style="{StaticResource ModernSearchBox}" x:Name="txtSearch"/>
                    <TextBlock IsHitTestVisible="False" Text="Search client or loan ID..." VerticalAlignment="Center" HorizontalAlignment="Left" Margin="40,0,0,0" Foreground="#A0AEC0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text, ElementName=txtSearch}" Value="">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>

                <!-- Status Filter -->
                <ComboBox Style="{StaticResource ElegantComboBox}" x:Name="cmbStatus" Width="140" Margin="15,0,0,0">
                    <ComboBoxItem Content="All Statuses" IsSelected="True"/>
                    <ComboBoxItem Content="Active"/>
                    <ComboBoxItem Content="Paid"/>
                    <ComboBoxItem Content="Past Due"/>
                </ComboBox>

                <!-- Plan Filter -->
                <ComboBox Style="{StaticResource ElegantComboBox}" x:Name="cmbPlan" Width="160" Margin="15,0,0,0">
                    <ComboBoxItem Content="All Plans" IsSelected="True"/>
                    <ComboBoxItem Content="Daily"/>
                    <ComboBoxItem Content="Semimonthly"/>
                    <ComboBoxItem Content="Monthly"/>
                    <ComboBoxItem Content="Quarterly"/>
                    <ComboBoxItem Content="Anually"/>
                </ComboBox>
            </StackPanel>

            <!-- Add Button -->
            <Button Grid.Column="1" Style="{StaticResource ElegantPrimaryButtonStyle}" x:Name="btnAddLoan" HorizontalAlignment="Center" Width="164" Click="btnAddLoan_Click_1">
                <StackPanel Orientation="Horizontal">
                    <fa:ImageAwesome Icon="PlusCircle" Width="14" Height="14" Foreground="White" Margin="0,0,10,0"/>
                    <TextBlock Text="New Loan Record" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Main Content Area -->
        <Grid Grid.Row="2" Margin="0,1,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Layout Switcher Tab -->
            <Border BorderBrush="#E2E8F0" BorderThickness="1" Margin="30,0,30,15" CornerRadius="10" HorizontalAlignment="Left" Background="White">
                <Grid Width="240" Height="38">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Button x:Name="btnCardView" Content="Card View" Click="Button_Click" Cursor="Hand" Background="#F1F5F9">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource CardButtonStyle}">
                                <Setter Property="Foreground" Value="#3044FF"/>
                            </Style>
                        </Button.Style>
                    </Button>
                    
                    <Button x:Name="btnTableView" Grid.Column="1" Content="Table View" Click="Button_Click_1" Cursor="Hand" Background="Transparent">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource CardButtonStyle}">
                                <Setter Property="Foreground" Value="#64748B"/>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- Content -->
            <Grid Grid.Row="1">
                <ScrollViewer x:Name="scrollCards" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Visibility="Visible">
                    <WrapPanel x:Name="loanContainer" Orientation="Horizontal" HorizontalAlignment="Center" Margin="20,0,20,20"/>
                </ScrollViewer>
                
                <Frame x:Name="loanFrame" NavigationUIVisibility="Hidden" Visibility="Collapsed" Margin="30,0,30,20"/>
            </Grid>
        </Grid>

        <!-- Footer -->
        <StackPanel Grid.Row="3" Margin="30,10,30,25">
            <Separator Background="#E2E8F0" Height="1" Margin="0,0,0,15"/>
            <TextBlock Text="© 2026 TWLH Loan Management System | System v1.0" 
                       HorizontalAlignment="Center" Foreground="#64748B" FontSize="11"/>
        </StackPanel>
    </Grid>
</Page>
